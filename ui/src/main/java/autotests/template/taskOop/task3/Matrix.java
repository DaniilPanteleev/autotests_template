package autotests.template.taskOop.task3;

public class Matrix {
    int[][] numbers;
    int line = numbers.length;
    int column = numbers[0].length;

    public void plus(Matrix one, Matrix two){

        for (int i = 0; i < one.line || i < two.line; i++){
            for(int j = 0; j < one.column || j < two.column; j++){

            }
        }
    }
}
